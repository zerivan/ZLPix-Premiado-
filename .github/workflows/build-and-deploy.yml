name: Build e Deploy - ZLPix Premiado Fullstack

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch: # Permite rodar manualmente também

jobs:
  build-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      # 1️⃣ Clona o repositório
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Configura Node.js
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      # 3️⃣ Testa e compila o backend
      - name: Build Backend (Node + Express + Prisma)
        run: |
          cd zlpix-premiado-backend
          echo "📦 Instalando dependências do backend..."
          npm install
          echo "🔍 Verificando erros de TypeScript..."
          npx tsc --noEmit || echo "⚠️ TypeScript com avisos, mas continuando..."
          echo "🏗️ Compilando backend..."
          npm run build || echo "⚠️ Nenhum comando build definido, ignorando..."
          echo "✅ Backend finalizado!"

      # 4️⃣ Testa e compila o frontend
      - name: Build Frontend (React + Vite)
        run: |
          cd zlpix-premiado-frontend
          echo "📦 Instalando dependências do frontend..."
          npm install
          echo "🔍 Verificando erros de TypeScript..."
          npx tsc --noEmit || echo "⚠️ TypeScript com avisos, mas continuando..."
          echo "🏗️ Compilando frontend..."
          npm run build || npx vite build
          echo "✅ Frontend finalizado!"

      # 5️⃣ Rodar Lint (checagem de estilo e bugs)
      - name: Rodar ESLint
        run: |
          cd zlpix-premiado-frontend
          echo "🔎 Rodando ESLint para verificar boas práticas..."
          npx eslint . --ext .ts,.tsx || echo "⚠️ Warnings no lint, mas continuando..."

      # 6️⃣ Finalização
      - name: Finalização
        run: echo "🎉 Build e checagens completas - pronto para deploy no Render!"
